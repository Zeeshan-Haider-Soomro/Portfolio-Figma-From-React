

const Button = ({text}) => {
  return (
    <div className="">
        <button className="bg-white text-black hover:border-white border-2 hover:bg-black hover:text-white rounded-full p-3  font-semibold">{text}</button>
    </div>
  )
}

export default Button